# AI 主播陪练 · 项目专属约束

> 本文件在外层 `AGENTS.md`（核心规则卡）之上，补充本项目专属约定。冲突时以外层核心规则卡为准。

## 产品边界（来自 PRD v1.5）
- 形态：第一版为电脑网页端，只服务个人主播与新人主播；机构端移至后续版本。
- 核心闭环：选直播类型/训练目标（可选填主题、商品、脚本）→ 摄像头麦克风模拟直播（预设关键问题 + 动态 AI 弹幕）→ 练后带时间点反馈 → 重练 / 前后对比进步。
- 三入口并列：看教程 / 练一个难点 / 完整模拟，不设强制学习顺序。

## 强制底线补充（本项目特有）
- AI 不自动判定主播能否上播；上播资格由带教老师人工决定（PRD 5、6.2）。
- 不编造用户未提供的商品功效、价格、承诺；缺资料时标明未知、不做具体商品事实核验。
- 动态弹幕不得脱离训练目标、不得伪装为真实观众；界面必须标注为"模拟观众"。
- 镜头提醒只做客观项（是否入镜、画面是否清楚），不输出眼神/表情/手势/感染力等主观评分。
- 音视频（含人脸、声音）默认按境内处理；能否发第三方模型、是否用于训练、留存期限以决策台账 D7 与《PRD 补全清单》问题 1 的结论为准，未确认前不发送。
- 个人自主练习录像默认仅本人可见；权限判断在后端，前端隐藏不算数。
- 摄像头/麦克风被拒绝时，必须给出可操作提示，不得进入"假装正在录制"的状态。

## 状态约定
- 训练状态机、录像/转写/弹幕事件/反馈/重练记录的持久化方式，由阶段 1 技术适配声明和阶段技术开发文档定义；本文件不预设。
- 宿主程序管权限、预算、超时降级和事实版本；模型只负责当前步骤内容，不得自行改授权或状态。

## 当前授权范围
- 已进入阶段 3 前后端核心闭环接入。已授权接通创建练习、实时转写与动态弹幕、录像上传、训练反馈、重练和前后对比。
- 教程、成长记录、录像列表、个人资料与真实登录仍保留前端模拟数据，补充后端接口前不得把模拟数据描述为真实用户数据。
